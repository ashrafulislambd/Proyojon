
  # Buy Now Pay Later Webapp

  This is a code bundle for Buy Now Pay Later Webapp. The original project is available at https://www.figma.com/design/hZZb72lB4Zclbjgl6C0RFj/Buy-Now-Pay-Later-Webapp.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  